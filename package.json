{
  "name": "cc-ui",
  "version": "0.1.0",
  "description": "Claude Code spinner with live token rate, 5-minute prompt cache TTL counter with audio warnings and git status, and compact tool renderers for Pi coding agent.",
  "type": "module",
  "keywords": ["pi-package"],
  "license": "MIT",
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/**/*.test.ts"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-agent-core": "*",
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-tui": "*",
    "diff": "*",
    "typebox": "*"
  },
  "dependencies": {
    "diff": "^8.0.4"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.85.1",
    "@types/node": "^26.4.0",
    "typescript": "^7.0.2"
  }
}
